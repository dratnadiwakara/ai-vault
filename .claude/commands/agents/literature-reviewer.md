---
description: Reads converted markdowns from related-papers/mds/, builds a BibTeX file, writes per-paper summaries, drafts a structured literature review, and triggers a critique. Run after /agents/literature-downloader has finished acquiring PDFs. Only invoked via the /agents/literature-reviewer slash command. Do NOT trigger based on intent inference or keywords.
---

You are running the **literature-reviewer** agent. Your inputs are the markdown files already present in `related-papers/mds/` (produced by `/agents/literature-downloader`). You do **not** download PDFs or run any conversion — assume that work is already done.

## Phase 0: Verify inputs

1. List all `*.md` files in `related-papers/mds/`. If the folder is empty or missing, stop and tell the user:
   > "`related-papers/mds/` contains no markdown files. Please run `/agents/literature-downloader` first."

2. Define constants:
   - `MD_DIR   = "related-papers/mds"`
   - `BIB_PATH = "related-papers/references.bib"`
   - `REVIEW   = "related-papers/literature-review.md"`
   - `CRITIQUE = "related-papers/literature-review-critique.md"`

---

## Phase 1: Build BibTeX

For each `*.md` file in `related-papers/mds/`:

1. Read the markdown (or enough of it to see the title page, abstract, and reference list).
2. Infer bibliographic fields from the visible text:
   - `title` — typically the first prominent heading or large text block on the first page.
   - `author` — the author line immediately after the title; parse as `Last, First` separated by `and`.
   - `year` — from copyright footer, journal header, or citation block.
   - `journal` / `booktitle` — from running header/footer or title page phrases ("Journal of …", "NBER Working Paper", etc.).
   - `volume`, `number`, `pages` — only if clearly visible.
   - `publisher` — for books, from title page.
3. Use the file's stem (without `.md`) as the BibTeX key.
4. Choose entry type: `@article`, `@book`, `@incollection`, or `@misc` (fallback for working papers).
5. Omit any field that cannot be confidently inferred — do not guess.

Write `related-papers/references.bib` (create or overwrite). Include a comment block at the top:

```bibtex
% Auto-generated by /agents/literature-reviewer from related-papers/mds/
% Review and correct entries manually before submission.
```

Ensure valid BibTeX syntax: comma-separated fields, strings in `{}`, no trailing comma after the last field.

---

## Phase 2: Summarize papers

For each `*.md` in `related-papers/mds/`:

- Read enough to understand: research question, data and sample, empirical/theoretical approach, main findings, notable limitations.
- **Long markdowns** (equivalent of >~15 pages): launch parallel general-purpose subagents, one per ~5-page chunk of text, each producing a short chunk summary. When all return, synthesize into a single 2–5 sentence paper summary.
- **Short markdowns**: read the abstract, introduction, and conclusion directly.
- Draft a 2–5 sentence summary that covers:
  - What the study asks and why it matters.
  - How it answers the question (design, model, or data).
  - Key results and any notable limitations.
- Link each summary to the paper's BibTeX key.

Retain all summaries in memory for use in Phase 3.

---

## Phase 3: Draft the literature review

1. **Identify themes**: group the papers into 2–5 thematic clusters based on topic, method, or mechanism. Assign each paper to at least one theme.

2. **Write `related-papers/literature-review.md`** (create or overwrite) using this structure:

```markdown
# Literature Review: [Topic inferred from papers]

**Date**: [Today's date]
**Number of Papers Reviewed**: [N]

## Overview

[1–3 paragraphs summarizing the overall state of the literature, major themes,
and why this body of work matters.]

## Thematic Discussion

### Theme 1: [Descriptive name]

[Short paragraph introducing the theme.]

- **[BibKey] Author et al. (Year)**: [Summary — question, method, findings, limitations.]
- **[BibKey] Author & Author (Year)**: [Summary...]

### Theme 2: [Descriptive name]

[...]

## Gaps and Directions for Future Work

[What is missing, tensions across findings, and promising directions,
based only on the reviewed papers.]

## References

[Formatted reference list drawn only from `references.bib`. For each entry:
Authors. (Year). Title. Journal/Source. BibTeX key: [key].]
```

- The References section must cite only keys present in `references.bib`.
- Write as a continuous narrative, not just bullet lists.

---

## Phase 4: Critique

1. Launch a **general-purpose subagent** with the following role:

   > You are a **senior researcher** evaluating the quality of a literature review.
   > Read `related-papers/literature-review.md` and `related-papers/references.bib`.
   > Assess:
   > - Coverage: are major strands well represented given the papers available?
   > - Thematic organization and coherence.
   > - Balance and fairness in summarizing results.
   > - Clarity and readability.
   > - Quality and correctness of the references section relative to the .bib file.
   >
   > Provide:
   > - A short overall assessment (2–4 sentences).
   > - 3–8 major comments (structure, themes, missing connections, misinterpretations).
   > - 5–15 minor comments (clarity, style, ordering, small corrections).
   > - Concrete, actionable suggestions for improving the review.

2. Save the critic's output as `related-papers/literature-review-critique.md` with these headings:
   - `## Overall Assessment`
   - `## Major Comments`
   - `## Minor Comments`
   - `## Suggested Revisions`

---

## Final report to user

Summarize:
- Number of papers processed.
- Paths to:
  - `related-papers/references.bib`
  - `related-papers/literature-review.md`
  - `related-papers/literature-review-critique.md`
- Top 3 issues raised by the critic.
